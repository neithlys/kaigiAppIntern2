import { StyleSheet } from 'react-native'

const IconStyles = StyleSheet.create({
    logout: {
        padding: 10,
        margin: 5,
        height: 28,
        width: 40,
        resizeMode: 'stretch',
    },
})
export default IconStyles
